---
import { posthogSnippet } from "../lib/posthog-snippet.mjs";
const key = import.meta.env.PUBLIC_POSTHOG_KEY;
const host = import.meta.env.PUBLIC_POSTHOG_HOST || "https://eu.i.posthog.com";
const snippet = posthogSnippet(key, host);
---
{snippet && <script is:inline set:html={snippet} />}
