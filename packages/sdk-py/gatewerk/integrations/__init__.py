"""Optional framework integrations for the Gatewerk Python SDK.

Each integration lives in its own subpackage and depends on optional extras
(e.g. ``pip install gatewerk[langgraph]``). The base ``gatewerk`` import does
not load any integration modules.
"""
